NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# www is canonical. Used for metadataBase, canonical links, sitemap, RSS/JSON
# feeds, and OG tags — every generated URL must match the host the site
# actually resolves at.
NEXT_PUBLIC_SITE_URL=https://www.equity.tw
